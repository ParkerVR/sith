# Sith Application Dependencies
#
# Standard library modules:
# - subprocess (system commands, built-in)
# - json (config management, built-in)
# - datetime (time tracking, built-in)
# - typing (type hints, built-in)
#
# External packages:
pyobjc-framework-Cocoa>=12.1  # For native macOS Cocoa framework and UI
pyobjc-framework-Quartz>=12.1  # For idle time detection (sandbox-compatible)
setproctitle>=1.3.0  # For setting process name in Activity Monitor
#
# Python 3.7+ is recommended.
